/* takes a pointer to an int as a parameter
 * and updates the value it points to to 98
 */
{
#include "main.c"

void reset_to_98(int *n)
	int n = 98;
	int *p - &n;
	printf(value to 98: %d\n", *p);

	return 0;
}
